function [np, re] = lagrange_interpolation(f,xx)

dd = divided_difference(f, xx);
np = nested_polynomial(dd, xx(1:end-1));

y = subs(f,xx);
y_lagrange = subs(np,xx);

re = abs(y - y_lagrange) ./ y;

y - y_lagrange 

end
